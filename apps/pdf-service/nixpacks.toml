[phases.setup]
nixPkgs = ["...", "nodejs-20_x", "google-chrome", "atk", "at-spi2-atk", "cups", "libdrm", "libxkbcommon", "mesa", "pango"]

[phases.build]
cmds = ["npm install"]

[start]
cmd = "npm start"
